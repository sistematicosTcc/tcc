// import {useHistory, useParams} from "react-router-dom" 

export const Verificação = () =>{
  // const history = useHistory();
  // const {email} = useParams

  return (
    <>
    <h1>Verificando minha conta</h1> {/* //{email} */}
      {/* <button to={`/login/${email}`}>Confirmar</button> */}
    </>
  );
}

export default Verificação;
